// external headers
#include <ktx.h>

// project headers
#include "Lettuce/Core/AssetLoader.hpp"

using namespace Lettuce::Core;

TextureData AssetLoader::loadTexture(std::string name)
{
    TextureData data;

    return data;
}